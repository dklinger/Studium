<?php

echo "Anzahl der Zahlen: ";
$anz = trim(fgets(STDIN));

$arr = array();

for($i=1;$i<=$anz;$i++)
{
	echo "Zahl $i: ";
	$zahl = trim(fgets(STDIN));
	if(is_numeric($zahl))
	{
		$arr[] = $zahl;
	}
}
	
?>