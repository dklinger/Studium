<?php

$dir = "C:\Arbeit\Studium\xampp portable\htdocs";
$dir_content = GetContent($dir);



function GetContent($dir, $dirElements)
{
	for ($i = 0; $i < $directory.length; $i++)
	{
		if(id_dir($directory[$i]))
		{
			print()
			GetContent($directory ."\". $directory[$i]);
		}
		else 
		{
			print
		}
	}
}

?>